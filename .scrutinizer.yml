# .scrutinizer.yml

build:
    tests:
        override:
            - phpcs-run

checks:
    php: true

filter:
    paths:
        - "src/*"
    excluded_paths:
        - "benchmark/"
        - "build/"
        - "examples/"
        - "tests/"
        - "vendor/"
